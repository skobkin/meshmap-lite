package buildinfo

// AppName is the user-facing application name shown in logs and the UI.
const AppName = "MeshMap Lite"

// Version is overridden at build time for tagged releases and defaults to dev.
var Version = "dev"
